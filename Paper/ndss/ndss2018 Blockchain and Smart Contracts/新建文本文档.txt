去中心化的基于路径交易的网络在参与者之间维持本地交易通道。用户对利用这些通道来处理交易通过不需要记录所有交易的路径中介。去中心化PBT网络的核心是一个路由算法，算法发现发送者和接受者之间的路径。近些年提出了一些路径算法，包括……然而缺乏效率或者隐私性，综合考虑在实践中必须保证PBT网络是不可缺少的。
本文，首先定义了现有PBT算法的几个效率关注点。基于此设计评估了SpeedyMurmurs,一个新的算法。还用了真实数据。